<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>🌳 Интерактивное Древо</h1>
  <%= link_to "К списку персон", group_ancestors_path(@group), class: "btn btn-outline-primary" %>
</div>

<div class="row">
  <div class="col-md-9">
    <div class="card shadow-sm" style="min-height: 600px; background-color: #fcfcfc; background-image: radial-gradient(#d7d7d7 1px, transparent 1px); background-size: 20px 20px;">
      <div class="card-body text-center" style="overflow-x: auto;">

        <% if @chart_data.include?("-->") || @ancestors.count > 0 %>
          <div class="mermaid">
            <%= @chart_data.html_safe %>
          </div>
        <% else %>
          <div class="text-muted mt-5">
            <h3 style="color: #adb5bd;">Холст пуст</h3>
            <p>Добавьте связи в панели справа, чтобы здесь появилось древо.</p>
          </div>
        <% end %>

      </div>
    </div>
  </div>

  <div class="col-md-3">
    <div class="card shadow-sm mb-4">
      <div class="card-header bg-light">
        <h5 class="mb-0 m-1">🔗 Создать связь</h5>
      </div>
      <div class="card-body">
        <p class="text-muted small">Выберите двух персон и укажите, кем они приходятся друг другу.</p><a href=""></a>

        <%= form_with url: make_connection_group_path(@group), method: :post, local: true do |f| %>
          <div class="mb-3">
            <label class="form-label fw-bold">Кто (Ребенок):</label>
            <%= f.select :child_id, options_from_collection_for_select(@ancestors, :id, :full_name), { prompt: "Выберите персону..." }, { class: "form-select", required: true } %>
          </div>

          <div class="mb-3 text-center">
            <span class="badge bg-secondary">⬆️ является ребенком для ⬆️</span>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold">Родитель:</label>
            <%= f.select :parent_id, options_from_collection_for_select(@ancestors, :id, :full_name), { prompt: "Выберите родителя..." }, { class: "form-select", required: true } %>
          </div>

          <div class="mb-3">
            <label class="form-label fw-bold">Роль:</label>
            <%= f.select :role, [["Это Мать", "mother"], ["Это Отец", "father"]], {}, { class: "form-select" } %>
          </div>

          <%= f.submit "Связать персон", class: "btn btn-primary w-100" %>
        <% end %>

      </div>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/mermaid@9.4.3/dist/mermaid.min.js"></script>
<script>
    mermaid.initialize({ startOnLoad: true, theme: 'default' });
</script>