<h1>Мои группы</h1>

<%= link_to 'Создать группу', new_group_path %>

<% @groups.each do |group| %>
  <div>
    <h3><%= link_to group.name, group %></h3>
    <p><%= group.description %></p>
    <p>Тип: <%= group.privacy %></p>
  </div>
<% end %>
