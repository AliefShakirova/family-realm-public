<div class="row justify-content-center">
  <div class="col-md-8">
    <div class="card shadow-sm">
      <div class="card-body">
        <h2 class="card-title text-center mb-4">Edytowanie miejsca</h2>

        <%= render "form", location: @location %>

        <div class="text-center mt-3">
          <%= link_to "Anulowanie", group_location_path(@group, @location), class: "text-muted" %>
        </div>
      </div>
    </div>
  </div>
</div>