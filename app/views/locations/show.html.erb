<div class="card">
  <div class="card-header d-flex justify-content-between align-items-center">
    <h2 class="mb-0"><%= @location.name %></h2>
    <div>
      <%= link_to "Редактировать", edit_group_location_path(@group, @location), class: "btn btn-sm btn-outline-primary" %>
      <%= button_to "Удалить",
                    group_location_path(@group, @location),
                    method: :delete,
                    class: "btn btn-sm btn-danger ms-2",
                    form: { style: 'display:inline-block;' } %>
    </div>
  </div>

  <div class="card-body">
    <% if @location.address.present? %>
      <p class="lead"><strong>📍 Адрес:</strong> <%= @location.address %></p>
    <% end %>

    <hr>

    <h5>Описание</h5>
    <% if @location.description.present? %>
      <p style="white-space: pre-wrap;"><%= @location.description %></p>
    <% else %>
      <p class="text-muted">Описание отсутствует.</p>
    <% end %>

    <hr class="mt-5">
    <h4>События в этом месте:</h4>
    <p class="text-muted">Пока событий нет (функционал в разработке).</p>

  </div>
</div>

<div class="mt-3">
  <%= link_to "← К списку мест", group_locations_path(@group), class: "btn btn-outline-secondary" %>
</div>